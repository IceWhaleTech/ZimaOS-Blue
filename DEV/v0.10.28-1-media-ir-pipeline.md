# PRD: IR-Based Media Generation Pipeline

**Version**: 0.10.28-1
**Author**: ZimaOS-Blue Team
**Status**: Draft
**Created**: 2026-02-23

---

## 1. Overview

### 1.1 Problem Statement

The current media generation flow routes through the LLM chat pipeline:

```
User message → LLM → tool_use(image_generate/video_generate) → MediaManager → Provider → Result
```

This has several issues:

1. **Unnecessary LLM round-trip** — generating an image from "draw me a cat" doesn't need LLM reasoning; it wastes tokens and adds 2-5s latency
2. **Tool-use fragility** — the LLM may not always invoke the correct tool, or may hallucinate parameters
3. **No user control** — users can't choose the model, resolution, or style before generation starts
4. **No graceful interruption** — once the LLM decides to generate, there's no way to cancel and fall back to chat
5. **Limited model catalog** — MuleRouter now offers 25+ models across 4 vendors; the tool-use approach can't expose this richness

### 1.2 Proposed Solution

Replace the LLM tool-use path with an **Intent Recognition (IR) classifier** that runs _before_ the message enters the LLM pipeline:

```
User message → IR Classifier → [Media Intent?]
  YES → Interactive Parameter UI → Direct MediaManager call → Placeholder + Progress → Result
  NO  → Normal LLM chat pipeline
```

### 1.3 Goals

1. Detect media generation intent without calling the LLM (zero-token classification)
2. Present an interactive parameter selection UI before generation starts
3. Route directly to `MediaManager` bypassing the chat/tool-use pipeline
4. Show a real-time placeholder (progress bar, thumbnail preview) during generation
5. Allow users to cancel generation and fall back to normal chat at any time
6. Expose the full MuleRouter model catalog (25+ models across text-to-image, text-to-video, image-to-video, image editing)

### 1.4 Non-Goals

- This does NOT replace the existing tool-use path (kept as fallback for complex multi-step workflows)
- This does NOT add new media providers — it leverages existing `MediaManager` + `MuleRouterProvider`
- This does NOT handle audio generation (separate feature)
- No prompt engineering or enhancement in this version (MuleRouter's `prompt_extend` handles this)

### 1.5 Target Users

| User | Need |
|------|------|
| End users | Fast, intuitive media generation with model/parameter choice |
| Power users | Fine-grained control over resolution, duration, style, seed |
| Developers | Clean API separation between chat and media generation paths |

---

## 2. Intent Classification (IR Layer)

### 2.1 Media Intent Categories

| Category | ID | Description | Requires Image Input |
|----------|----|-------------|---------------------|
| Text-to-Image | `t2i` | Generate image from text prompt | No |
| Text-to-Video | `t2v` | Generate video from text prompt | No |
| Image-to-Video | `i2v` | Animate a reference image | Yes |
| Image Editing | `i2i` | Modify/transform an existing image | Yes |
| Keyframe-to-Video | `kf2v` | Generate video from first+last frame | Yes (2 images) |

### 2.2 Classification Approach

Use a **rule-based IR classifier** (no LLM needed) with two layers:

**Layer 1: Keyword + Pattern Matching**
- Regex patterns for explicit generation requests across all 27 supported locales
- Action verbs + media nouns per language family:

| Language Group | Locales | Action Verbs (generate/create/draw) | Media Nouns (image/video) |
|---------------|---------|-------------------------------------|--------------------------|
| English | en-US, en-GB, ga-IE | `generate\|create\|draw\|paint\|make\|produce\|render` | `image\|picture\|photo\|illustration\|video\|animation\|clip` |
| Chinese | zh-CN, zh-TW | `生成\|画\|绘\|制作\|创建\|做\|弄` | `图\|图片\|照片\|图像\|视频\|动画\|影片` |
| Japanese | ja-JP | `生成\|描く\|作る\|作成\|描いて\|書いて` | `画像\|写真\|イラスト\|動画\|ビデオ\|映像\|アニメ` |
| Korean | ko-KR | `생성\|그려\|만들\|그리다\|제작` | `이미지\|사진\|그림\|영상\|동영상\|비디오` |
| German | de-DE | `erstellen\|erzeugen\|zeichnen\|malen\|generieren` | `Bild\|Foto\|Illustration\|Video\|Animation` |
| French | fr-FR | `générer\|créer\|dessiner\|peindre\|produire` | `image\|photo\|illustration\|vidéo\|animation` |
| Spanish | es-ES, ca-ES | `generar\|crear\|dibujar\|pintar\|producir` | `imagen\|foto\|ilustración\|vídeo\|animación` |
| Portuguese | pt-BR, pt-PT | `gerar\|criar\|desenhar\|pintar\|produzir` | `imagem\|foto\|ilustração\|vídeo\|animação` |
| Italian | it-IT | `generare\|creare\|disegnare\|dipingere\|produrre` | `immagine\|foto\|illustrazione\|video\|animazione` |
| Dutch | nl-NL | `genereren\|maken\|tekenen\|schilderen` | `afbeelding\|foto\|illustratie\|video\|animatie` |
| Russian | ru-RU | `сгенерировать\|создать\|нарисовать\|сделать` | `изображение\|картинку\|фото\|видео\|анимацию` |
| Polish | pl-PL | `wygeneruj\|stwórz\|narysuj\|zrób` | `obraz\|zdjęcie\|ilustrację\|wideo\|animację` |
| Swedish | sv-SE | `generera\|skapa\|rita\|måla` | `bild\|foto\|illustration\|video\|animation` |
| Danish | da-DK | `generer\|opret\|tegn\|mal` | `billede\|foto\|illustration\|video\|animation` |
| Norwegian | nb-NO | `generer\|lag\|tegn\|mal` | `bilde\|foto\|illustrasjon\|video\|animasjon` |
| Czech | cs-CZ | `vygeneruj\|vytvoř\|nakresli` | `obrázek\|fotku\|ilustraci\|video\|animaci` |
| Slovak | sk-SK | `vygeneruj\|vytvor\|nakresli` | `obrázok\|fotku\|ilustráciu\|video\|animáciu` |
| Hungarian | hu-HU | `generálj\|készíts\|rajzolj\|alkoss` | `képet\|fotót\|illusztrációt\|videót\|animációt` |
| Romanian | ro-RO | `generează\|creează\|desenează` | `imagine\|fotografie\|ilustrație\|video\|animație` |
| Greek | el-GR | `δημιούργησε\|ζωγράφισε\|φτιάξε` | `εικόνα\|φωτογραφία\|βίντεο\|κινούμενο` |
| Croatian | hr-HR | `generiraj\|stvori\|nacrtaj` | `sliku\|fotografiju\|ilustraciju\|video\|animaciju` |
| Malayalam | ml-IN | `സൃഷ്ടിക്കുക\|വരയ്ക്കുക\|ഉണ്ടാക്കുക` | `ചിത്രം\|ഫോട്ടോ\|വീഡിയോ\|ആനിമേഷൻ` |

- Negation filter (per-language): "don't generate", "不要生成", "描かないで", "nicht erstellen", etc. → NOT media intent
- Question filter: "how to generate images?", "怎么生成图片？", "画像の作り方" → NOT media intent (informational)
- Case-insensitive matching; Unicode-aware word boundaries

**Layer 2: Context Signal Boosting**
- User attached an image → boost `i2v` / `i2i` confidence
- User attached 2 images → boost `kf2v` confidence
- Keywords like "animate", "move", "come alive" + image → strong `i2v` signal
- Keywords like "edit", "change", "modify", "remove" + image → strong `i2i` signal
- Keywords like "wallpaper", "portrait", "landscape" without image → strong `t2i` signal

**Layer 3: Ambiguity Resolution**
- If confidence < threshold (e.g., 0.7), return `ambiguous` → show disambiguation UI
- If multiple categories match, rank by specificity and present top choices

### 2.3 IR Classifier API

```go
// server/internal/mediagen/ir.go

type MediaIntent struct {
    Category    string  // "t2i", "t2v", "i2v", "i2i", "kf2v"
    Confidence  float64 // 0.0 - 1.0
    Prompt      string  // extracted/cleaned prompt text
    HasImage    bool    // user provided reference image(s)
    ImageCount  int     // number of attached images
    Suggestions []ModelSuggestion // recommended models for this intent
}

type ModelSuggestion struct {
    ModelID     string
    ModelName   string
    Vendor      string
    Reason      string // why this model is suggested
    IsDefault   bool
}

func ClassifyMediaIntent(message string, hasImages bool, imageCount int, locale string) *MediaIntent
```

---

## 3. Model Catalog & Routing

### 3.1 Full MuleRouter Model Catalog

Based on the MuleRouter API (https://mulerouter.ai/docs/api-reference):

**Text-to-Image (t2i)**

| Model ID | Vendor | Name | Notes |
|----------|--------|------|-------|
| `dall-e-3` | openai | DALL-E 3 | Sync, high quality |
| `qwen-image-max` | alibaba | Qwen Image Max | Async, complex text rendering |
| `nano-banana-pro` | google | Nano Banana Pro | Async, ultra-sharp |
| `midjourney` | midjourney | Midjourney | Async, artistic style |
| `wan2.5-t2i-preview` | alibaba | Wan 2.5 T2I | Async, preview |
| `wan2.6-t2i` | alibaba | Wan 2.6 T2I | Async |

**Text-to-Video (t2v)**

| Model ID | Vendor | Name | Notes |
|----------|--------|------|-------|
| `wan2.6-t2v` | alibaba | Wan 2.6 T2V | Async, high quality |
| `wan2.5-t2v-spark` | mulerouter | Wan 2.5 T2V Spark | Async, fast |
| `wan2.6-t2v-spark` | mulerouter | Wan 2.6 T2V Spark | Async, fast |
| `wan2.5-t2v-preview` | alibaba | Wan 2.5 T2V | Async, preview |
| `wan2.2-t2v-plus` | alibaba | Wan 2.2 T2V Plus | Async |

**Image-to-Video (i2v)**

| Model ID | Vendor | Name | Notes |
|----------|--------|------|-------|
| `wan2.6-i2v` | alibaba | Wan 2.6 I2V | Async, high quality |
| `wan2.5-i2v-spark` | mulerouter | Wan 2.5 I2V Spark | Async, with audio |
| `wan2.6-i2v-spark` | mulerouter | Wan 2.6 I2V Spark | Async, with audio |
| `wan2.5-i2v-preview` | alibaba | Wan 2.5 I2V | Async, preview |
| `wan2.2-i2v-plus` | alibaba | Wan 2.2 I2V Plus | Async |
| `wan2.2-i2v-flash` | alibaba | Wan 2.2 I2V Flash | Async, 50% faster |
| `midjourney-video` | midjourney | Midjourney Video | Async |

**Image Editing (i2i)**

| Model ID | Vendor | Name | Notes |
|----------|--------|------|-------|
| `qwen-image-edit-max` | alibaba | Qwen Image Edit | Multi-image, style transfer |
| `wan2.5-i2i-preview` | alibaba | Wan 2.5 I2I | Async, preview |
| `wan2.6-image` | alibaba | Wan 2.6 Image | Text+reference editing |
| `nano-banana-pro` (edit) | google | Nano Banana Edit | Text-based editing |

**Keyframe-to-Video (kf2v)**

| Model ID | Vendor | Name | Notes |
|----------|--------|------|-------|
| `wan2.1-kf2v-plus` | alibaba | Wan 2.1 KF2V | First+last frame |
| `wan2.1-vace-plus` | alibaba | Wan 2.1 VACE | Multi-modal video editing |

### 3.2 Model Selection Logic

When the IR classifier identifies an intent category, the system suggests models based on:

1. **Category match** — only show models that support the detected category
2. **Provider availability** — only show models from configured+enabled providers
3. **Locale preference** — CN users default to Alibaba models; others default to OpenAI/Google
4. **Quality vs Speed** — "spark" and "flash" variants are faster; "plus" and "max" are higher quality
5. **User history** — remember last-used model per category (stored in localStorage)

---

## 4. Interactive Parameter UI

### 4.1 UI Flow

When media intent is detected, instead of sending to LLM, the frontend shows an **inline parameter panel** below the user's message:

```
┌─────────────────────────────────────────────────┐
│ 🎨 Image Generation Detected                    │
│                                                  │
│ Prompt: "a cat sitting on a rainbow"             │
│                                                  │
│ Model:  [DALL-E 3 ▾]  [Midjourney ▾]  [Qwen ▾] │
│                                                  │
│ Size:   [1024×1024 ▾]                            │
│ Quality: [Standard ▾]                            │
│ Style:  [Natural ▾]                              │
│                                                  │
│ [Generate]              [Switch to Chat Instead] │
└─────────────────────────────────────────────────┘
```

For video generation with image input:

```
┌─────────────────────────────────────────────────┐
│ 🎬 Image-to-Video Detected                      │
│                                                  │
│ Prompt: "the cat starts running"                 │
│ Reference: [cat.jpg thumbnail]                   │
│                                                  │
│ Model:     [Wan 2.6 I2V Spark ▾]                │
│ Resolution: [720P ▾]  [1080P ▾]                  │
│ Duration:  [5s ▾]  [10s ▾]                       │
│ Audio:     [Auto ▾]  [Custom ▾]  [None ▾]        │
│                                                  │
│ [Generate]              [Switch to Chat Instead] │
└─────────────────────────────────────────────────┘
```

### 4.2 Parameter Definitions by Category

**t2i Parameters:**
- `model` — model selector (filtered by t2i capability)
- `size` — resolution (model-dependent options)
- `quality` — standard / hd (model-dependent)
- `style` — natural / vivid (model-dependent)
- `n` — number of images (1-4)
- `negative_prompt` — optional, collapsible "Advanced" section

**t2v Parameters:**
- `model` — model selector (filtered by t2v capability)
- `resolution` — 720P / 1080P
- `duration` — 5s / 10s
- `prompt_extend` — toggle (default on)
- `seed` — optional, in "Advanced" section

**i2v Parameters:**
- `model` — model selector (filtered by i2v capability)
- `resolution` — 720P / 1080P
- `duration` — 5s / 10s
- `audio` — auto / custom URL / none
- `prompt_extend` — toggle

**i2i Parameters:**
- `model` — model selector (filtered by i2i capability)
- `size` — output resolution
- `n` — number of outputs (1-6 for qwen-image-edit-max)

### 4.3 "Switch to Chat" Escape Hatch

The "Switch to Chat Instead" button:
1. Dismisses the parameter panel
2. Sends the original message through the normal LLM chat pipeline
3. The LLM may or may not invoke media tools — user gets full conversational control

This is critical for cases where the IR classifier is wrong (e.g., "draw me a diagram of the architecture" is NOT an image generation request).

---

## 5. Generation Pipeline (Task-Based)

### 5.1 Backend API

New endpoint that creates a persistent task and returns immediately:

```
POST /api/v1/media/generate
Content-Type: application/json

{
  "category": "t2i",
  "prompt": "a cat on a rainbow",
  "model": "nano-banana-pro",
  "params": {
    "size": "1024x1024",
    "quality": "hd",
    "n": 1
  },
  "reference_images": ["data:image/jpeg;base64,..."],
  "message_id": "msg-abc123",
  "source": "web"
}
```

Response (202 Accepted — immediate):

```json
{
  "task_id": "xxx-yyy-zzz",
  "message_id": "msg-abc123",
  "status": "pending",
  "category": "t2i",
  "model": "nano-banana-pro"
}
```

Poll for status:

```
GET /api/v1/media/tasks/:id
```

Get tasks by message:

```
GET /api/v1/media/tasks/by-message/:message_id
```

### 5.2 Task State Machine

```
pending → processing → succeeded (terminal)
                     → failed    (terminal)
                     → cancelled (terminal)
```

- Shared lifecycle via `task.BaseTask` (embedded in `MediaTask`) — see §7.4
- Status enum: `task.StatusPending`, `task.StatusProcessing`, `task.StatusSucceeded`, `task.StatusFailed`, `task.StatusCancelled`
- `status.IsTerminal()` helper — returns true for succeeded/failed/cancelled
- Tasks are persisted to SQLite (`media_tasks` table) before execution starts
- On power failure, `RecoverTasks()` resumes non-terminal tasks on startup
- Tasks with `upstream_id` resume polling; tasks without re-execute from scratch
- Terminal states are never re-executed

### 5.3 Web vs Channel Rendering

| Source | Behavior |
|--------|----------|
| `web` | Frontend polls `GET /tasks/:id` until terminal state, then renders result inline |
| `channel` | Backend async watcher polls until complete, then sends result to channel with interval recovery |

### 5.4 Chat Session Integration

The conversation shows only the task ID. The generated media is associated with the `message_id` internally. When the task completes:
- Web: frontend renders the result in the chat placeholder
- Channel: backend sends the result as a channel message

---

## 6. Frontend Placeholder & Progress

### 6.1 Placeholder States

During generation, show an inline placeholder in the chat:

**State 1: Pending**
```
┌─────────────────────────────┐
│  ⏳ Generating image...      │
│  Model: DALL-E 3             │
│  [Cancel]                    │
└─────────────────────────────┘
```

**State 2: Processing (with progress)**
```
┌─────────────────────────────┐
│  🔄 Generating video... 45%  │
│  ████████░░░░░░░░░░  45%    │
│  Model: Wan 2.6 I2V Spark   │
│  Elapsed: 23s                │
│  [Cancel]                    │
└─────────────────────────────┘
```

**State 3: Complete**
```
┌─────────────────────────────┐
│  [Generated image/video]     │
│  Model: DALL-E 3 · 3.2s     │
│  [Regenerate] [Edit Prompt]  │
└─────────────────────────────┘
```

**State 4: Failed**
```
┌─────────────────────────────┐
│  ❌ Generation failed         │
│  Error: Rate limit exceeded  │
│  [Retry] [Switch to Chat]   │
└─────────────────────────────┘
```

### 6.2 Cancel & Fallback

The "Cancel" button during generation:
1. Aborts the HTTP request / SSE stream
2. Shows a cancelled state with options:
   - [Retry with same params]
   - [Switch to Chat] — sends original message to LLM pipeline
   - [Try different model]

---

## 7. Technical Architecture

### 7.1 Component Diagram

```
Frontend (Vue)                          Backend (Go)
─────────────                          ─────────────
ChatInput
  │
  ├─ onSend()
  │   ├─ classifyIntent() ◄──────────── POST /api/v1/media/classify
  │   │   (local JS first,               (server-side fallback)
  │   │    server fallback)
  │   │
  │   ├─ [media intent?]
  │   │   YES → show ParamPanel
  │   │   NO  → normal chat send
  │   │
  │   └─ ParamPanel
  │       ├─ model selector
  │       ├─ param controls
  │       └─ [Generate] ──────────────► POST /api/v1/media/generate
  │                                      │
  │                                      ├─ Create PersistentTask (SQLite)
  │                                      ├─ Return task_id immediately (202)
  │                                      └─ Start async: provider.Generate()
  │                                           └─ pollTask() → UpdateStatus()
  │
  └─ MediaPlaceholder
      ├─ poll GET /tasks/:id ──────────► GetTask (memory + DB fallback)
      ├─ render progress
      └─ render result on terminal state

Channel (Discord/Slack/etc)             Backend (Go)
───────────────────────                ─────────────
  message received
  │
  ├─ classifyIntent()
  ├─ POST /api/v1/media/generate (source: "channel")
  │   └─ Returns task_id
  │
  └─ Async watcher polls until complete
      └─ Sends result to channel
```

### 7.2 New Files

**Backend:**
- `server/internal/mediagen/ir.go` — Intent classifier (rule-based)
- `server/internal/mediagen/ir_test.go` — Classifier tests (72 cases)
- `server/internal/mediagen/task_store.go` — SQLite task persistence
- `server/internal/mediagen/task_store_test.go` — Task store tests (5 cases)
- `server/internal/mediagen/skill_image.go` — Image generation skill (renamed from tool_image.go)
- `server/internal/mediagen/skill_video.go` — Video generation skill (renamed from tool_video.go)
- `server/internal/mediagen/channel_watcher.go` — Channel task watcher (polls until complete, notifies channel)
- `server/internal/mediagen/channel_watcher_test.go` — Channel watcher tests (4 test functions)
- `server/internal/mediagen/interceptor.go` — MediaInterceptor for channel IR classification
- `server/internal/task/task.go` — Shared base task types: `BaseTask`, `Status` enum, lifecycle methods
- `server/internal/task/store.go` — Time conversion helpers for SQLite persistence
- `server/internal/task/task_test.go` — Base task unit tests (6 cases)
- `server/internal/sockipc/server.go` — Unix socket IPC listener
- `server/internal/sockipc/protocol.go` — Key=value line protocol parser/writer
- `server/internal/sockipc/handler.go` — IPC command dispatcher
- Handler additions in `handler.go`:
  - `POST /api/v1/media/classify` — classify intent (server-side)
  - `POST /api/v1/media/generate` — create persistent task, return immediately
  - `GET /api/v1/media/tasks/by-message/:message_id` — tasks by message

**Frontend:** ✅
- `web/src/components/MediaParamPanel.vue` — Interactive parameter selection + ambiguity resolution UI
- `web/src/components/MediaPlaceholder.vue` — Generation progress placeholder (ARIA-accessible)
- `web/src/composables/useMediaIntent.ts` — Client-side IR classifier (mirrors Go logic, 4 languages)
- `web/src/composables/useMediaGenerate.ts` — Task polling + SSE streaming + model memory + retry + multi-image
- `web/src/api/media.ts` — Extended with `classifyIntent`, `directGenerate`, `getTasksByMessage` + IR types

### 7.3 Integration Points

- `ChatView.vue` — intercept `handleSend()` to run IR classification, show `MediaParamPanel` above input
- `ChatMessage.vue` — render `MediaPlaceholder` for in-progress generations (planned)
- `chat.go` — `ProcessChannelMessage` intercepts media intent via `MediaInterceptor` before LLM call
- `routes.go` — register new media endpoints, create `ChannelTaskWatcher` + `Interceptor`, wire to `ChatHandler`
- `main.go` — wire `ChannelTaskWatcher.SetNotifier()` to `channelManager.Send()`
- `handler.go` — add classify + generate handlers

### 7.4 Socket IPC Architecture

LLM skills and CLI commands communicate with the main process via a Unix domain socket, avoiding SQLite multi-process issues and providing a stable text-based protocol suitable for LLM tool output.

**Socket Path:** `{dataDir}/blue.sock`

**Protocol:** Key=value per line, blank line terminates request, response follows same format.

```
# Request (skill/CLI → main process)
cmd=media.generate
token=abc123sha256hash
session_id=sess-xxx
message_id=msg-yyy
category=t2i
model=nano-banana-pro
prompt=a cat on a rainbow
size=1024x1024

# Response (main process → skill/CLI)
status=ok
task_id=task-zzz

```

**Supported Commands:**

| Command | Description | Key Parameters |
|---------|-------------|----------------|
| `media.generate` | Create media generation task | category, model, prompt, size, resolution, duration |
| `media.status` | Query task status | task_id |
| `media.cancel` | Cancel a running task | task_id |
| `ping` | Health check | (none) |

**Auth Context:**
- `token` — SHA256 hash lookup in `api_keys` table (same auth as CLI/HTTP)
- `session_id` — associates task with chat session
- `message_id` — associates task with specific message (for frontend rendering)

**Why Unix Socket + Key=Value (not HTTP/JSON):**
1. LLM tool output is text-based — JSON generation is fragile (unescaped quotes, missing braces)
2. Key=value per line is trivially parseable and LLM-stable
3. Unix socket has no port conflicts, lower overhead than HTTP
4. `cat request.txt | socat - UNIX-CONNECT:blue.sock` works for debugging

**Component Flow:**
```
LLM Skill (tool call)
  │
  ├─ writes key=value to blue.sock
  │
  └─► sockipc.Server (goroutine per connection)
        │
        ├─ parse key=value lines
        ├─ validate token (SHA256 → api_keys table)
        ├─ dispatch to handler by cmd
        │   └─ media.generate → MediaManager.CreateTask()
        │
        └─ write response key=value lines
```

**New Files:**
- `server/internal/sockipc/server.go` — Unix socket listener, connection handler, graceful shutdown
- `server/internal/sockipc/handler.go` — Command dispatcher, media command handlers
- `server/internal/sockipc/protocol.go` — Key=value parser/writer

---

## 8. Implementation Checklist

### Phase 1: IR Classifier (P0) ✅
- [x] 1.1 Create `server/internal/mediagen/ir.go` — rule-based classifier with 22 language keyword tables
- [x] 1.2 Implement 3-layer classification: keyword matching → context signal boosting → ambiguity resolution
- [x] 1.3 Word boundary detection for short Latin words (avoids "move" inside "remove")
- [x] 1.4 CJK-aware substring matching (no word boundaries needed for ideographic scripts)
- [x] 1.5 Negation filter (per-language) and question filter (per-language)
- [x] 1.6 English fallback for mixed-language input
- [x] 1.7 Create `server/internal/mediagen/ir_test.go` — 72 test cases across EN/CN/JA/KO + 20 European languages
- [x] 1.8 `POST /api/v1/media/classify` endpoint in handler.go
- [x] 1.9 Client-side JS/TS classifier (mirror of Go logic for zero-latency) — `web/src/composables/useMediaIntent.ts`

### Phase 2: Model Catalog & Task-Based Generation (P0) ✅
- [x] 2.1 Add `Category` field to `MediaModelInfo` in types.go
- [x] 2.2 Expand `MuleRouterProvider.SupportedModels()` — 25 models with category tags (t2i/t2v/i2v/i2i/kf2v)
- [x] 2.3 Update `modelToVendor()` for new spark models (wan2.5/2.6 spark variants)
- [x] 2.4 Category filter on `GET /api/v1/media/models?category=t2i`
- [x] 2.5 Rename `tool_image.go` → `skill_image.go`, `tool_video.go` → `skill_video.go` (types: `*Skill`)
- [x] 2.6 `POST /api/v1/media/generate` — creates persistent task, returns task ID immediately (no SSE)
- [x] 2.7 `task_store.go` — SQLite-backed task persistence (`media_tasks` table) with state machine
- [x] 2.8 `PersistentTask` ↔ `MediaTask` conversion (JSON serialization for request/response)
- [x] 2.9 `Manager.CreateTask()` — persist to DB first, then start async execution
- [x] 2.10 `Manager.RecoverTasks()` — resume non-terminal tasks on startup (power-failure recovery)
- [x] 2.11 `Manager.GetTasksByMessage()` + `GET /tasks/by-message/:message_id` endpoint
- [x] 2.12 `MediaTask` extended with `MessageID`, `Category`, `Source` fields
- [x] 2.13 Bootstrap wiring: `NewTaskStore(db)` + `SetTaskStore()` + `RecoverTasks()` in routes.go
- [x] 2.14 `task_store_test.go` — 5 test cases (CRUD, status updates, ListPending, GetByMessageID, conversion)

### Phase 2.5: Task Abstraction (P0) ✅
- [x] 2.5.1 Create `server/internal/task/task.go` — `BaseTask` struct, `Status` enum (Pending/Processing/Succeeded/Failed/Cancelled), `IsTerminal()`, `SetFailed()`, `SetCompleted()`, `SetProgress()`
- [x] 2.5.2 Create `server/internal/task/store.go` — `TimeToSQL`/`TimeFromSQL`/`NullTimeToSQL`/`NullTimeFromSQL` helpers
- [x] 2.5.3 Create `server/internal/task/task_test.go` — 6 test cases (IsTerminal, SetFailed, SetCompleted, SetProgress clamping, time conversions)
- [x] 2.5.4 Refactor `mediagen/types.go` — embed `task.BaseTask` in `MediaTask`, `TaskStatus = task.Status` alias
- [x] 2.5.5 Refactor `mediagen/task_store.go` — use `task` time helpers, `status.IsTerminal()`
- [x] 2.5.6 Update struct literals in manager.go, dashscope.go, gemini.go, mulerouter.go — `BaseTask: task.BaseTask{...}`
- [x] 2.5.7 All 85 tests pass (6 task + 79 mediagen), go vet clean

### Phase 2.6: Unix Socket IPC Layer (P0)
- [x] 2.6.1 Create `server/internal/sockipc/server.go` — Unix socket listener at `{dataDir}/blue.sock`, goroutine-per-connection, graceful shutdown
- [x] 2.6.2 Create `server/internal/sockipc/protocol.go` — key=value line parser/writer (blank line = end of message)
- [x] 2.6.3 Create `server/internal/sockipc/handler.go` — command dispatcher: `media.generate`, `media.status`, `ping` + token auth
- [x] 2.6.4 Token auth via APIKeyService.ValidateKey (same as CLI auth)
- [x] 2.6.5 Wire socket server into bootstrap: start on server init, close on graceful shutdown via `deps.Closers`
- [x] 2.6.6 Create `assets/skills/mediagen/SKILL.md` — embedded into binary via `go:embed`
- [x] 2.6.7 Remove `image_generate`/`video_generate` tool registrations (IR pipeline replaces LLM tool-use path)
- [x] 2.6.8 23 unit tests pass (10 protocol + 5 server + 8 handler), go vet clean
- [x] 2.6.9 CLI `blue media generate` subcommand — writes key=value to socket, reads response (`server/cmd/blue/media.go`)
- [x] 2.6.10 End-to-end integration test: socket → main process → task created (`sockipc/e2e_test.go`, 2 tests)

### Phase 3: Frontend Parameter Panel & Polling (P0)
- [x] 3.1 `web/src/components/MediaParamPanel.vue` — model selector, category badge, generate button
- [x] 3.2 Integrate into `ChatView.vue` send flow (intercept handleSend, classify intent)
- [x] 3.3 "Switch to Chat Instead" escape hatch (dismiss button on panel)
- [x] 3.4 Model memory (localStorage last-used per category) — in `useMediaGenerate.ts`
- [x] 3.5 `web/src/api/media.ts` — added `classifyIntent`, `directGenerate`, `getTasksByMessage` + IR types
- [x] 3.6 Frontend polling loop: SSE streaming via `streamMediaTask` + fallback `pollTask` in `useMediaGenerate.ts`
- [x] 3.7 `web/src/components/MediaPlaceholder.vue` — pending/progress/complete/failed states with progress bar

### Phase 4: Channel Async Notification (P1) ✅
- [x] 4.1 Channel source detection: `source: "channel:<name>:<chatID>"` encoded in task, IR classifier intercept in `ProcessChannelMessage`
- [x] 4.2 Async task watcher (`ChannelTaskWatcher`) — goroutine-per-task polling until terminal state, then notifies channel via `channel.Manager.Send()`
- [x] 4.3 Interval-based recovery: `RecoverChannelTasks()` re-watches non-terminal channel tasks after restart
- [x] 4.4 Channel notification format: `FormatChannelResult()` — image/video URLs with category label and model name

### Phase 5: Polish & Edge Cases (P1) ✅
- [x] 5.1 Ambiguity resolution UI — when classifier confidence < 0.7, shows disambiguation prompt ("Did you mean to generate media?") with confirm/dismiss buttons
- [x] 5.2 Multi-image input handling — image files stored in `pendingImageFiles`, converted to base64 data URLs in `generate()`, passed as `reference_images`
- [x] 5.3 Error recovery and retry logic — `retry()` function in `useMediaGenerate`, retry button in `MediaPlaceholder` for failed/cancelled tasks
- [x] 5.4 i18n for parameter labels and status messages (en-US, zh-CN) — `media` section in locale files
- [x] 5.5 Accessibility: ARIA roles/labels on all interactive elements, `role="progressbar"` with `aria-valuenow`, `role="alert"` for errors, `aria-live="polite"` for status updates, `alt` text for generated images

---

## 9. API Reference (MuleRouter)

Base URL: `https://api.mulerouter.ai`

### 9.1 Endpoint Pattern

**Generation (async):**
```
POST /vendors/{vendor}/v1/{model}/generation
Authorization: Bearer {api_key}
Content-Type: application/json
```

**Task Polling:**
```
GET /vendors/{vendor}/v1/{model}/generation/{task_id}
Authorization: Bearer {api_key}
```

### 9.2 Vendor Mapping

| Vendor | Models |
|--------|--------|
| `openai` | dall-e-3 (sync, uses `/v1/images/generations`) |
| `alibaba` | qwen-*, wan2.2-*, wan2.5-*, wan2.6-* |
| `google` | nano-banana-pro |
| `midjourney` | midjourney (`/tob/diffusion`), midjourney-video (`/tob/video-diffusion`) |
| `mulerouter` | wan2.5-*-spark, wan2.6-*-spark |

### 9.3 Common Request Fields

| Field | Type | Description |
|-------|------|-------------|
| `prompt` | string | Generation prompt (max 2000 chars) |
| `negative_prompt` | string? | Unwanted content |
| `image` | string? | Reference image (URL or base64) |
| `size` | string? | Output size (e.g., "1024x1024") |
| `resolution` | string? | "720P" or "1080P" (video) |
| `duration` | int? | Video length in seconds (5 or 10) |
| `audio` | bool? | Auto-generate audio (default true) |
| `audio_url` | string? | Custom audio file URL |
| `prompt_extend` | bool? | Intelligent prompt rewriting (default true) |
| `seed` | int? | Random seed (0-2147483647) |
| `n` | int? | Number of outputs |

### 9.4 Response Format

**Creation (202 Accepted):**
```json
{
  "task_info": {
    "id": "uuid",
    "status": "pending",
    "created_at": "ISO-8601",
    "updated_at": "ISO-8601"
  }
}
```

**Polling (200 OK — in progress):**
```json
{
  "task_info": { "id": "uuid", "status": "processing" },
  "progress": 0.45
}
```

**Polling (200 OK — complete):**
```json
{
  "task_info": { "id": "uuid", "status": "succeeded" },
  "output": {
    "results": [
      { "url": "https://...", "type": "image" }
    ]
  }
}
```

---

## 10. Success Metrics

| Metric | Target |
|--------|--------|
| IR classification accuracy | > 90% on test corpus |
| False positive rate (non-media classified as media) | < 5% |
| Time from send to parameter panel | < 100ms (client-side) |
| Time from "Generate" click to first progress event | < 2s |
| User engagement with parameter panel vs auto-generate | Track ratio |
| "Switch to Chat" usage rate | < 15% (indicates good IR accuracy) |

---

## 11. Open Questions

1. **Should the IR classifier also run server-side?** Client-side gives instant feedback, but server-side ensures consistency. Current plan: both, with client-side as primary.
2. **How to handle prompt enhancement?** MuleRouter's `prompt_extend` does this, but should we also offer a "enhance prompt" button that uses the LLM?
3. **Model pricing display?** MuleRouter has per-model pricing — should we show estimated cost before generation?
4. **Batch generation?** Should users be able to queue multiple generations with different params?
5. **History/gallery view?** Should there be a dedicated page for all generated media (beyond chat history)?
