# v0.10.29 Autonomous Agent — Checklist

**Version**: 0.10.29
**Created**: 2026-02-25

---

## Phase 1: Dynamic Tool Rounds + Agent Mode Toggle

- [x] Replace `const maxToolRounds = 5` with dynamic `getMaxToolRounds()` (5 normal / 1000 agent)
- [x] Add `AgentMode *bool` field to `Settings` struct in `settings_handler.go`
- [x] Add `GetAgentMode()` accessor method
- [x] Add `agent_mode` to PATCH handler
- [x] Add `agentMode` + `agentModeFunc` to `SystemPromptBuilder`
- [x] Add `SetAgentMode()` / `SetAgentModeFunc()` / `isAgentMode()` methods
- [x] Add `buildAgentModeGuidance()` — agent mode system prompt with auto-verification
- [x] Inject agent mode guidance in `Build()` and `BuildWithContext()`
- [x] Wire `SetAgentModeFunc(settingsHandler.GetAgentMode)` in `bootstrap/routes.go`
- [x] Update all 3 tool loop sites to use `h.getMaxToolRounds()`

## Phase 2: Background Autonomous Tasks (Agent Loop)

- [x] Create `server/internal/agent/task.go` — types (Task, PlanStep, TaskStatus, etc.)
- [x] Create `server/internal/agent/store.go` — SQLite store (CRUD, CountRunning, Cleanup)
- [x] Create `server/internal/agent/runner.go` — AgentRunner (Submit, Cancel, execute, generatePlan, executeStep)
- [x] Create `server/internal/agent/handler.go` — REST API (CreateTask, ListTasks, GetTask, CancelTask, DeleteTask)
- [x] Wire agent into `bootstrap/routes.go` (store + runner + handler + routes)
- [x] Add tool definitions to agent runner LLM calls (registry → llmTools() → ChatRequest.Tools)
- [x] Add auto-verification step after plan execution
- [x] Add agent task tests (`agent/runner_test.go`) — 17 tests pass

## Phase 3: MCP Server

- [x] Create `server/internal/mcp/server.go` — JSON-RPC protocol (initialize, tools/list, tools/call, ping)
- [x] Create `server/internal/mcp/handler.go` — SSE + HTTP transport
- [x] Wire MCP into `bootstrap/routes.go`
- [x] Add `resources/list` and `resources/read` methods (workspace files)
- [x] Wire workspace reader into MCP server
- [x] Add MCP server tests (`mcp/server_test.go`) — 16 tests pass

## Phase 4: Frontend — Agent Task UI

- [x] Create `web/src/components/AgentTaskPanel.vue` — task progress panel
- [x] Add agent API client to `web/src/api/chat.ts` (agentApi)
- [x] Add `agent_mode` to `web/src/api/settings.ts` Settings interface
- [x] Add i18n strings: `en-US.ts` (agent.mode, agent.taskCreated, etc.)
- [x] Add i18n strings: `zh-CN.ts` (agent.mode, agent.taskCreated, etc.)
- [x] Integrate AgentTaskPanel into ChatView.vue (task list + rendering)
- [x] Add agent mode toggle button to chat header
- [x] Add `agentMode` computed + `setAgentMode` action to settings store

## Integration & Polish

- [x] Pass tool definitions in agent runner's LLM ChatRequest
- [x] Handle `tools` field in agent runner `executeStep`
- [x] Unlimited tool rounds in agent mode (1000 cap)
- [x] Auto-verification guidance in system prompt
- [x] Auto-verification step in agent runner
- [x] Verify full build (`go build ./cmd/bluelib/`) ✓
- [x] Run existing tests — agent/mcp/tools all pass ✓
