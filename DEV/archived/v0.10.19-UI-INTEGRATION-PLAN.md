# v0.10.19 UI集成计划

## 当前状态
- ✅ SpeechSettings.vue 已集成到SettingsView
- ✅ TTSSettings.vue 已存在
- ❌ 新的TTS组件未集成

## 需要集成的组件

1. **PrivacyConsentDialog.vue** - 隐私同意对话框
   - 在首次使用Edge-TTS时显示
   - 需要在Chat或主应用中集成

2. **ProviderSelector.vue** - 提供商选择器
   - 可集成到SpeechSettings中

3. **VoicePackManager.vue** - 语言包管理
   - 可集成到SpeechSettings中

4. **VoiceCustomizer.vue** - 音色定制
   - 可集成到SpeechSettings中

## 集成步骤

### 1. 更新SpeechSettings.vue
- 导入新组件
- 添加提供商选择
- 添加语言包管理
- 添加音色定制

### 2. 在App.vue中集成PrivacyConsentDialog
- 全局显示隐私对话框
- 首次使用Edge-TTS时触发

### 3. 更新API调用
- 使用新的tts.ts API
- 集成Pinia store

## 优先级
1. 高: ProviderSelector集成到SpeechSettings
2. 高: VoiceCustomizer集成到SpeechSettings
3. 中: VoicePackManager集成到SpeechSettings
4. 中: PrivacyConsentDialog集成到App.vue
