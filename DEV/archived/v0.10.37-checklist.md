# v0.10.37 Checklist

**Version**: 0.10.37  
**Release Date**: 2026-04-01  
**Archived Date**: 2026-04-02  
**Status**: Archived; runtime-trace contract hardening, skill market security label sync, Harness subagent visualization V1, Automation IA migration, and locale coverage landed with targeted validation green  
**Focus**: Consolidate the 0.10.37 runtime follow-up and the later web/operator polish into one release record without changing the external chat/task projection model

---

## Overview

v0.10.37 is a narrow follow-up release after the larger runtime-convergence work recorded in v0.10.36.

This version does not try to reopen the whole architecture. Instead, it closes a specific remaining gap:

- Blue had a stronger shared runtime bundle than before, but it still lacked a clearly exposed single in-process runtime trace contract
- Harness had tracing and observer groundwork, but operator-facing surfaces could not yet consume that runtime trace directly
- Subagent execution had stronger shared wiring, but the isolation story still needed clearer metadata and lifecycle boundaries
- Progressive loading existed as a desired skill-system direction, but runtime-safe budget hooks and regression gates were not yet explicit enough

The release intent for v0.10.37 is therefore simple:

- Keep chat, agent, harness, research, subagent, approval, and memory behavior externally stable
- Add only additive contract fields and internal runtime providers
- Expose runtime trace data through the existing harness control plane
- Improve runtime guardrails and regression visibility without introducing a new platform model

---

## Scope Guardrails ✅

- [x] Do not slim `tool schema` in this cycle
- [x] Do not re-open the high-risk `tool -> skill + exec` migration path in this cycle
- [x] Keep existing REST and SSE contracts backward compatible
- [x] Allow only additive response fields on operator surfaces
- [x] Avoid database schema migration work
- [x] Avoid changing Deep Research wait and non-wait behavior
- [x] Avoid changing approval interaction semantics
- [x] Keep `skill` and `tool` canonical identifiers underscore-based
- [x] Limit the cycle to runtime contract hardening, trace exposure, boundary tests, and low-risk operator visibility improvements

---

## Why This Version Exists

The remaining architectural gap after v0.10.36 was not only route size or runtime wiring. It was contract clarity.

At the end of the previous cycle:

- Harness already had runtime observers and trace collection primitives
- Skill selection had moved closer to contract-based behavior
- The runtime bundle existed as the correct integration center
- Operator surfaces still primarily consumed evidence fragments rather than a single trace-oriented runtime contract

v0.10.37 addresses that gap by making runtime traces a first-class additive output of the harness control plane while also hardening the surrounding execution boundaries.

---

## Implementation Checklist

## 1. Single Runtime Trace Contract ✅

- [x] Introduce a controller-level runtime trace provider contract instead of letting trace access remain a bootstrap-only detail
- [x] Make runtime trace snapshot access available through the harness control plane as a shared internal capability
- [x] Keep the trace provider optional so runtimes that do not wire tracing still remain valid
- [x] Preserve the existing event store and artifact store as the source of truth rather than creating a second trace database
- [x] Keep trace access additive and non-breaking from the client point of view

### What Landed

- [x] A single in-process runtime trace entrypoint now exists at the harness-controller layer
- [x] Trace collection remains backed by stored run events and attached artifacts
- [x] The shared runtime bundle now wires trace collection into the controller rather than treating it as a sidecar utility only

### Why It Matters

- [x] Blue now has a clearer answer to “what is the single runtime contract for execution trace data inside the process?”
- [x] The route layer no longer needs to know how trace snapshots are assembled
- [x] Future operator or compatibility surfaces can reuse the same contract instead of rebuilding trace logic independently

---

## 2. Harness Trace Exposure on Existing Operator Surfaces ✅

- [x] Add `run_trace` to run-detail responses as an additive field
- [x] Add `runtime_traces` to group-report responses as an additive field
- [x] Keep existing `events`, `artifacts`, and `runtime_evidence` behavior unchanged
- [x] Preserve the shape of existing payloads so old clients continue to work
- [x] Reuse linked-run identity as the join key for trace rendering

### What Landed

- [x] Run detail can now return one trace snapshot alongside events and artifacts
- [x] Group report can now return per-run trace snapshots for linked runs
- [x] The trace payload includes run identity, runtime status, stages, events, artifacts, and latency when available
- [x] Runtime evidence and runtime traces now coexist instead of competing for the same role

### Why It Matters

- [x] Runtime evidence is useful for semantic reasoning, but runtime traces are better for operator diagnosis
- [x] Blue now exposes a more contract-like runtime story without changing the existing public control-plane object family
- [x] This closes part of the remaining gap with architectures that treat middleware-chain and gateway-aligned runtime traces as first-class

---

## 3. Harness UI Runtime Trace Visibility ✅

- [x] Extend the harness group detail surface to read the new trace contract
- [x] Render runtime trace sections without disturbing the existing evidence, scorecard, checkpoint, and contract sections
- [x] Show stage count, event count, artifact count, and latency as lightweight operational summaries
- [x] Show stage-level details in a compact, operator-readable form
- [x] Keep the view additive so older reports with no trace data still render cleanly

### What Landed

- [x] Harness group detail now includes a dedicated runtime-trace section
- [x] Linked runs can now surface stage summaries, lifecycle events, and trace artifacts directly in the UI
- [x] Trace rendering gracefully degrades when trace data is absent
- [x] Existing evidence and scorecard diagnostics remain visible and unchanged

### Why It Matters

- [x] The harness control plane is now more obviously a runtime operations surface rather than only a report surface
- [x] Runtime trace data is now visible to humans without requiring backend-only inspection
- [x] The UI now reflects the same single trace contract used by the backend

---

## 4. Progressive Skill Loading Budget Controls ✅

- [x] Add token-budget-aware progressive loading behavior to skill selection
- [x] Keep the budget path optional so existing callers do not need an invasive signature change
- [x] Track loaded and skipped skills as part of selector decisions
- [x] Track budget usage, max budget, and trimming outcome in selector output
- [x] Support strict budget exhaustion as a structured selector failure mode while preserving partial decision state

### What Landed

- [x] Skill selection now supports runtime-friendly budget hints
- [x] Selector output can now explain which skills were loaded, which were skipped, and why trimming occurred
- [x] Strict budget mode now provides a clearer bounded failure path instead of only silent truncation

### Why It Matters

- [x] This is a practical first step toward first-class progressive skill loading without forcing the full deferred migration
- [x] Runtime budgeting is now more observable and testable
- [x] Blue can move toward stronger gateway or skill contracts with a lower risk profile

---

## 5. Runtime Contract Regression Gate ✅

- [x] Add a CI-facing drift test for the runtime contract shape
- [x] Use the runtime bundle as the source of truth for what the in-process harness contract exposes
- [x] Make contract drift a release-gate concern rather than a manual review concern only
- [x] Keep the gate low-risk by checking internal contract structure rather than changing external APIs

### What Landed

- [x] A runtime-contract CI gate now exists for the converged runtime bundle
- [x] Contract-shape drift is now easier to catch before follow-up refactors expand route or binder responsibilities again
- [x] Bootstrap wiring is now tested not just for existence, but for the intended contract outcome

### Why It Matters

- [x] Blue now has a more explicit answer to contract drift at the in-process boundary
- [x] This makes the runtime bundle more real as a platform object, even though it is still internal
- [x] It reduces the risk of route-layer regression after the earlier convergence pass

---

## 6. Subagent Isolation and Lifecycle Boundary Hardening ✅

- [x] Preserve harness-backed subagent execution as the shared runtime path
- [x] Make isolation metadata explicit on subagent results
- [x] Record parent-run identity clearly in subagent execution metadata
- [x] Add clearer subagent lifecycle events around start, completion, timeout, and wait behavior
- [x] Keep wait and non-wait behavior externally compatible

### What Landed

- [x] Subagent execution now marks isolation state explicitly
- [x] Parent and child execution relationship is easier to trace through run metadata and lifecycle events
- [x] Timeout and cancellation outcomes are more observable in the harness control plane
- [x] Subagent result handling is less ambiguous for downstream runtime consumers

### Why It Matters

- [x] Blue now treats harness-backed subagents more like first-class runtime children and less like loosely attached helper calls
- [x] Isolation is now an explicit runtime boundary instead of only an implementation assumption
- [x] This strengthens the long-term path toward clearer primary-agent and child-agent runtime contracts

---

## 7. Harness Tracing as Shared Runtime Infrastructure ✅

- [x] Keep tracing implemented through the existing execution middleware path
- [x] Record trace-requested, trace-started, and trace-start-failed lifecycle events
- [x] Allow trace snapshots to reconstruct stages, events, and artifacts from persisted run history
- [x] Keep the tracing layer lightweight and compatible with existing run storage semantics

### What Landed

- [x] Harness tracing now works both as runtime middleware and as an operator-readable snapshot source
- [x] The finalization stage is now visible in trace reconstruction when present
- [x] Trace artifacts participate in the same operator story as stage transitions and runtime events

### Why It Matters

- [x] This moves harness tracing from “instrumentation exists” to “instrumentation is part of the contract”
- [x] Blue can now reason about run execution more uniformly across backend, report, and UI surfaces

---

## Additional Merged Release Work ✅

### 8. Skill Market Security Label Sync

- [x] Ingest ClawHub `securityLabels` from both top-level and `latestVersion` fields and map them to `security_badge` for display and policy use
- [x] Add regression coverage for `securityLabels` parsing, extraction, and persisted-badge ingestion paths
- [ ] Confirm whether Tencent SkillHub (`lightmake`) exposes an equivalent upstream security badge field and ingest it if present

### 9. Harness Subagent Visualization V1

- [x] Extend the Harness web API surface with additive run, run-detail, and run-event types plus `listRuns`, `getRun`, and `getRunDetail`
- [x] Add deterministic linked-run tree construction for `linked_runs`, including coordinator detection, worker nesting, detached-worker grouping, and explicit front-end sorting
- [x] Replace the flat linked-runs block in Harness group detail with a coordinator/worker execution tree
- [x] Add a run detail inspector that lazy-loads per-run detail, caches it in-page, and opens from either tree selection or failed-item `Inspect run` actions
- [x] Support desktop side-inspector behavior and mobile bottom-sheet behavior without adding a new route
- [x] Keep existing group metrics, failed items, and artifact sections intact while adding the new execution visibility layer
- [x] Refresh selected active run detail silently on the same 4-second group refresh cadence while leaving terminal runs static
- [x] Add targeted regression coverage for tree building, detached-worker grouping, inspect-run detail opening, mobile sheet behavior, and active-vs-terminal detail refresh semantics

### 10. Harness Automation IA + Locale Coverage

- [x] Move Harness into the Automation information architecture as the canonical entry while keeping legacy `/harness` and `/security/harness/:id` routes as compatibility redirects
- [x] Update sidebar highlighting, page return paths, and Security migration affordances so Automation owns the Harness flow end to end
- [x] Add lightweight Chat visibility for Harness-backed work, including run status, verification status, evidence count, recording state, and quick actions
- [x] Fill all locale files with the new Harness automation naming, run-evaluation subtitle copy, research hover copy, partial-pass stage, and Harness task action/status labels

## Validation Summary ✅

### 1. Server Validation

- [x] Harness package regression suite passes
- [x] Bootstrap package regression suite passes
- [x] New run-detail trace coverage passes
- [x] New group-report trace coverage passes
- [x] Runtime-bundle trace-provider wiring coverage passes

### 2. Web Validation

- [x] Harness group detail view regression suite passes with runtime-trace coverage
- [x] The UI correctly renders additive runtime-trace payloads
- [x] The UI still renders older evidence-only payloads

### 3. Commands Run

- [x] `cd server && go test ./internal/harness`
- [x] `cd server && go test ./internal/bootstrap`
- [x] `cd web && npm run test:run -- src/__tests__/views/HarnessViews.test.ts`
- [x] `cd web && npm run test:run -- src/__tests__/i18n/HarnessLocaleOverrides.test.ts src/__tests__/i18n/DeepResearchLocales.test.ts`
- [x] `cd web && npm run test:run -- src/__tests__/utils/harnessRunTree.test.ts`
- [x] `cd web && npm run test:run -- src/__tests__/views/HarnessViews.test.ts`
- [x] `cd web && npx vue-tsc --noEmit --pretty false`

---

## Verification Checklist

- [x] Runtime trace provider is wired into the harness runtime bundle
- [x] Run-detail responses can expose a single trace snapshot
- [x] Group-report responses can expose per-linked-run trace snapshots
- [x] Trace exposure remains additive and backward compatible
- [x] Harness detail UI reads and renders the new trace contract
- [x] Progressive skill loading tracks loaded, skipped, and trimmed decisions
- [x] Strict skill budget exhaustion produces a structured bounded failure path
- [x] Runtime contract CI drift gate is present
- [x] Harness-backed subagent isolation metadata is explicit
- [x] Subagent lifecycle events now better reflect start, completion, timeout, and cancellation boundaries

---

## Release Notes Draft

### v0.10.37 - Runtime Trace Contract and Boundary Hardening

v0.10.37 is a targeted follow-up to the broader runtime-convergence work. It does not change the main public runtime model, but it makes the internal contract materially clearer.

What users and operators gain in practice:

- Harness-linked runs now expose runtime traces more directly
- The harness detail UI can show execution stages, lifecycle events, and trace artifacts
- Subagent execution carries clearer isolation and parent-child runtime semantics
- Skill selection has better progressive-loading budget observability
- Runtime contract drift is more strongly guarded in CI

What this version intentionally does not do:

- It does not slim tool schema
- It does not restart the full risky tool-surface migration effort
- It does not yet eliminate the independent research lane
- It does not yet make skill frontmatter and gateway-install contracts fully first-class

---

## Remaining Gaps After v0.10.37

- [ ] Research still remains a partially distinct runtime lane rather than a fully absorbed harness mode
- [ ] Skill frontmatter validation and gateway-install contract work still remain future work
- [ ] Progressive loading exists as budget-aware selector logic, but not yet as a fully universal runtime prompt-loading contract
- [ ] Blue still does not have the full skill-path, install-contract, and schema-validation maturity of a fully first-class gateway-native skill model
- [ ] The current runtime trace contract is additive and operator-friendly, but not yet a cross-runtime universal contract for every lane

---

## Archive Note

This document now serves as the single archived record for v0.10.37.

It consolidates two threads that landed in the same release window:

- single in-process runtime trace contract exposure
- harness operator-surface trace visibility
- progressive skill-loading budget controls
- runtime contract CI drift gating
- subagent isolation and lifecycle hardening
- skill market security badge sync and regression coverage
- Harness subagent execution-tree visualization and run-detail inspection
- Automation IA migration and locale coverage for Harness surfaces

It should be read as the archived 0.10.37 release delta on top of v0.10.36 rather than as a restatement of the full runtime-convergence archive.
