# CC Cache Enhancement — Implementation Checklist

**PRD**: PRD-0.10.25-cc-cache.md
**Version**: 0.10.25

---

## Phase 1: Canonicalizer & Singleflight (P0) ✅

- [x] 1.1 新建 `proxy/canonicalizer.go` — Sanitizer + Canonicalizer + SHA256 key 生成
- [x] 1.2 新建 `proxy/singleflight.go` — Singleflight wrapper 防穿透
- [x] 1.3 修改 `proxy/cache.go` — GenerateKey 改用 CanonicalKey，添加 L1/L2 分层 hit 统计
- [x] 1.4 修改 `proxy/handler.go` — ServeHTTP 集成 canonicalizer + singleflight + latency tracking

## Phase 2: L2 Disk Cache (P0) ✅

- [x] 2.1 新建 `proxy/cache_disk.go` — SQLite 磁盘缓存实现（复用已有依赖，无需新增 BadgerDB）
- [x] 2.2 修改 `proxy/cache.go` — CCCache 增加 L2 disk 字段，Get/Set 实现两级查找
- [x] 2.3 修改 `proxy/config.go` — DefaultCacheConfig 改为 multilevel 模式
- [x] 2.4 无需新增依赖（复用 mattn/go-sqlite3）

## Phase 3: Warmup & Metrics (P1) ✅

- [x] 3.1 Warmup 集成到 `proxy/cache.go` Warmup() 方法（从 L2 加载 Top-N 到 L1）
- [x] 3.2 修改 `proxy/cache.go` — 增强 Stats() 返回 l1_hit_ratio / disk_hit_ratio / latency_saved_ms
- [x] 3.3 修改 `proxy/cache_handler.go` — 添加 POST /warmup endpoint + TriggerWarmup handler
- [x] 3.4 warmup route 已通过 RegisterRoutes 自动注册
- [x] 3.5 修改 `cmd/blue/main.go` — 启动时自动 warmup + disk cache 路径使用 dataDir
- [x] 3.6 Singleflight 完整集成到 handler（cache miss 时合并并发请求）
- [x] 3.7 Cache HIT 时记录 latency_saved_ms

## Phase 3.5: Tests & Hardening ✅

- [x] 3.8 新建 `proxy/canonicalizer_test.go` — 15 个测试 + benchmark
- [x] 3.9 新建 `proxy/singleflight_test.go` — 6 个测试（并发去重、超时、context 取消、错误传播）
- [x] 3.10 新建 `proxy/cache_disk_test.go` — 11 个测试（CRUD、过期、TopN、headers 持久化）
- [x] 3.11 新建 `proxy/cache_integration_test.go` — 10 个测试（L1+L2 两级、promotion、warmup、stats）
- [x] 3.12 修复 `cache_disk.go` SQLite 时间比较 bug（datetime('now') → UTC RFC3339 参数）
- [x] 3.13 `handler.go` forwardAndCache 添加 SSE guard（防止上游意外返回 streaming 导致内存膨胀）

## Phase 4: Advanced (P2) — 后续版本

- [ ] 4.1 定时 Warmup (cron)
- [ ] 4.2 永久缓存（embedding/reference 类请求）
- [ ] 4.3 Prometheus / expvar 指标导出
- [ ] 4.4 前端缓存管理 UI

---

## 变更文件清单

**新建文件 (7)**:
| 文件 | 说明 |
|------|------|
| `proxy/canonicalizer.go` | Sanitizer + Canonicalizer + SHA256 key |
| `proxy/singleflight.go` | 自定义 singleflight（返回 *CCCacheEntry） |
| `proxy/cache_disk.go` | SQLite L2 磁盘缓存 |
| `proxy/canonicalizer_test.go` | Canonicalizer 单元测试 |
| `proxy/singleflight_test.go` | Singleflight 单元测试 |
| `proxy/cache_disk_test.go` | DiskCache 单元测试 |
| `proxy/cache_integration_test.go` | L1+L2 集成测试 |

**修改文件 (5)**:
| 文件 | 说明 |
|------|------|
| `proxy/cache.go` | L1+L2 两级架构、canonicalizer/singleflight/disk 字段、分层 Stats |
| `proxy/handler.go` | ServeHTTP 集成 singleflight + latency tracking + SSE guard |
| `proxy/config.go` | DefaultCacheConfig → multilevel 模式 |
| `proxy/cache_handler.go` | POST /warmup endpoint、L1+L2 delete |
| `cmd/blue/main.go` | 启动 warmup、dataDir 路径、目录创建 |
