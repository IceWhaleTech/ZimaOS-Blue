name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.21"
          cache-dependency-path: server/go.sum

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: web/package-lock.json

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Build Tauri desktop apps (optional, can be enabled when Tauri is set up)
  # tauri-build:
  #   name: Build Tauri App
  #   needs: release
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       include:
  #         - platform: macos-latest
  #           target: aarch64-apple-darwin
  #           name: macOS ARM64
  #         - platform: macos-latest
  #           target: x86_64-apple-darwin
  #           name: macOS x64
  #         - platform: windows-latest
  #           target: x86_64-pc-windows-msvc
  #           name: Windows x64
  #
  #   runs-on: ${{ matrix.platform }}
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Set up Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #
  #     - name: Set up Rust
  #       uses: dtolnay/rust-toolchain@stable
  #       with:
  #         targets: ${{ matrix.target }}
  #
  #     - name: Set up Go
  #       uses: actions/setup-go@v5
  #       with:
  #         go-version: "1.21"
  #
  #     - name: Install Tauri dependencies (Ubuntu)
  #       if: matrix.platform == 'ubuntu-latest'
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
  #
  #     - name: Build Go sidecar
  #       run: |
  #         cd server
  #         GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o ../tauri-app/src-tauri/bin/echo-server-${{ matrix.target }} ./cmd/echo
  #
  #     - name: Install frontend dependencies
  #       run: |
  #         cd tauri-app
  #         npm ci
  #
  #     - name: Build Tauri app
  #       uses: tauri-apps/tauri-action@v0
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       with:
  #         projectPath: tauri-app
  #         tagName: ${{ github.ref_name }}
  #         releaseName: "ZimaOS Echo ${{ github.ref_name }}"
  #         releaseBody: "See the assets to download this version."
  #         releaseDraft: false
  #         prerelease: false
