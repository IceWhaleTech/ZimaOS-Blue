# Notifications

Send, list, and manage system notifications.

## Usage

- "Send a notification: Build complete"
- "Show my notifications"
- "Clear notifications"

## Parameters

- `action`: send, list, clear, count
- `title`: Notification title
- `message`: Notification body
- `level`: info, warning, error (default: info)
