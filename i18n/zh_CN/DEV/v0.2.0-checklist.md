# v0.2.0 实施清单

[English Version](../../../DEV/v0.2.0-checklist.md)

本清单跟踪 v0.2.0（核心能力与最小功能）的实施进度。

## 目标
可以工作，但功能收敛。LLM 集成和基本 Agent 能力。

## 验收标准
- [x] 并发命令可控
- [x] P99 延迟稳定
- [x] 强制终止后安全恢复
- [x] 无 goroutine 泄漏

---

## 后端实施任务

### 1. LLM Provider 接口
- [x] 定义 provider 接口
- [x] OpenAI provider 实现
- [x] Claude (Anthropic) provider 实现
- [x] Ollama provider 实现（本地模型）
- [x] AWS Bedrock provider 实现 ✅
- [x] Provider 配置（API 密钥、端点）
- [x] 编写单元测试 ✅

### 2. 工具/函数调用框架
- [x] 定义工具接口
- [x] 工具注册表（静态注册）
- [x] 工具执行引擎
- [x] 内置工具：
  - [x] 计算器
  - [ ] 网页搜索（占位）- 推迟到 v0.4
  - [x] 文件操作（读/写）✅
  - [x] 系统信息
  - [x] 当前时间
- [x] 编写单元测试 ✅

### 3. 流式响应支持
- [x] SSE（Server-Sent Events）端点
- [x] 从 LLM provider 流式传输
- [x] 逐 token 传递
- [x] 流取消支持
- [x] 编写单元测试 ✅

### 4. 上下文管理
- [x] 对话上下文结构
- [x] 上下文窗口管理
- [x] Token 计数
- [x] 上下文截断策略
- [x] 编写单元测试 ✅

### 5. 记忆/对话历史 (SQLite)
- [x] SQLite 数据库设置
- [x] 对话模型
- [x] 消息模型
- [x] CRUD 操作
- [x] 对话搜索
- [x] 编写单元测试 ✅

### 6. 状态管理 (KV 存储)
- [x] KV 存储接口
- [x] 内存实现
- [x] SQLite 持久化
- [x] TTL 支持
- [x] 编写单元测试 ✅

### 7. 外部 API 封装
- [x] HTTP 客户端封装（在 LLM providers 中）
- [x] 速率限制（基础）
- [x] 超时处理
- [x] 重试逻辑 ✅
- [x] 编写单元测试 ✅

### 8. 聊天 API 端点
- [x] `POST /api/v1/conversations/:id/messages` - 发送消息
- [x] `POST /api/v1/conversations/:id/messages/stream` - 流式聊天
- [x] `GET /api/v1/conversations` - 列出对话
- [x] `GET /api/v1/conversations/:id` - 获取对话
- [x] `DELETE /api/v1/conversations/:id` - 删除对话
- [x] `GET /api/v1/providers` - 列出 LLM providers
- [x] `GET /api/v1/tools` - 列出可用工具
- [x] 编写集成测试 ✅

---

## 前端实施任务

### 1. 聊天界面
- [x] ChatView 组件 ✅
- [x] 消息列表组件 ✅
- [x] 消息输入组件 ✅
- [x] Markdown 渲染 ✅
- [x] 代码语法高亮 ✅

### 2. 流式支持
- [x] SSE 客户端 ✅
- [x] 实时消息更新 ✅
- [x] 输入指示器 ✅
- [x] 流取消 UI ✅

### 3. 对话管理
- [x] 对话列表侧边栏 ✅
- [x] 新建对话按钮 ✅
- [x] 对话删除 ✅
- [x] 对话搜索 ✅

### 4. 设置页面
- [x] LLM provider 选择 ✅
- [x] API 密钥配置 ✅
- [x] 模型选择 ✅
- [x] 温度/参数设置 ✅

### 5. 测试
- [x] Chat store 测试 ✅
- [x] Settings store 测试 ✅
- [x] SSE 客户端测试 ✅
- [x] Markdown 工具测试 ✅

---

## 技术要求

### 性能
- [x] 无 goroutine 泄漏（通过 pprof 验证）
- [x] 所有 IO 必须可中断
- [x] P99 延迟 < 100ms（不含 LLM 响应时间）

### 可靠性
- [x] 强制终止后安全恢复
- [x] 数据库完整性检查
- [x] LLM 不可用时优雅降级

### 安全性
- [ ] API 密钥静态加密 - 推迟到 v0.4
- [x] 输入验证
- [x] 每客户端速率限制 ✅

---

## 测试结果

### 后端
```
=== 服务端测试汇总 ===
llm:          4/4 通过 ✅
tools:        32/32 通过 ✅
streaming:    3/3 通过 ✅
context:      6/6 通过 ✅
memory:       7/7 通过 ✅
kvstore:      8/8 通过 ✅
chat:         5/5 通过 ✅
retry:        12/12 通过 ✅
ratelimit:    9/9 通过 ✅
--------------------------
总计:         86/86 通过 ✅
```

### 前端
```
=== Web 测试汇总 ===
stores/chat:      7/7 通过 ✅
stores/settings:  12/12 通过 ✅
utils/sse:        5/5 通过 ✅
utils/markdown:   14/14 通过 ✅
components:       5/5 通过 ✅
stores/system:    5/5 通过 ✅
--------------------------
总计:             48/48 通过 ✅
```

---

## 已创建文件

### 后端 (`server/`)
| 文件 | 状态 | 测试 |
|------|------|------|
| `internal/llm/provider.go` | ✅ | ✅ |
| `internal/llm/openai.go` | ✅ | ✅ |
| `internal/llm/claude.go` | ✅ | ✅ |
| `internal/llm/ollama.go` | ✅ | ✅ |
| `internal/llm/bedrock.go` | ✅ | ✅ |
| `internal/llm/llm_test.go` | ✅ | - |
| `internal/tools/registry.go` | ✅ | ✅ |
| `internal/tools/builtin.go` | ✅ | ✅ |
| `internal/tools/tools_test.go` | ✅ | - |
| `internal/streaming/sse.go` | ✅ | ✅ |
| `internal/streaming/sse_test.go` | ✅ | - |
| `internal/context/manager.go` | ✅ | ✅ |
| `internal/context/context_test.go` | ✅ | - |
| `internal/memory/sqlite.go` | ✅ | ✅ |
| `internal/memory/memory_test.go` | ✅ | - |
| `internal/kvstore/store.go` | ✅ | ✅ |
| `internal/kvstore/kvstore_test.go` | ✅ | - |
| `internal/server/chat.go` | ✅ | ✅ |
| `internal/server/chat_test.go` | ✅ | - |
| `internal/retry/retry.go` | ✅ | ✅ |
| `internal/retry/retry_test.go` | ✅ | - |
| `internal/ratelimit/ratelimit.go` | ✅ | ✅ |
| `internal/ratelimit/ratelimit_test.go` | ✅ | - |

### 前端 (`web/`)
| 文件 | 状态 | 测试 |
|------|------|------|
| `src/views/ChatView.vue` | ✅ | - |
| `src/views/SettingsView.vue` | ✅ | - |
| `src/components/ChatMessage.vue` | ✅ | - |
| `src/components/ChatInput.vue` | ✅ | - |
| `src/components/ConversationList.vue` | ✅ | - |
| `src/stores/chat.ts` | ✅ | ✅ |
| `src/stores/settings.ts` | ✅ | ✅ |
| `src/api/chat.ts` | ✅ | - |
| `src/utils/sse.ts` | ✅ | ✅ |
| `src/utils/markdown.ts` | ✅ | ✅ |

---

## API 端点（已实现）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| POST | `/api/v1/conversations` | 创建对话 | ✅ |
| GET | `/api/v1/conversations` | 列出所有对话 | ✅ |
| GET | `/api/v1/conversations/:id` | 获取对话详情 | ✅ |
| DELETE | `/api/v1/conversations/:id` | 删除对话 | ✅ |
| GET | `/api/v1/conversations/:id/messages` | 获取消息 | ✅ |
| POST | `/api/v1/conversations/:id/messages` | 发送消息 | ✅ |
| POST | `/api/v1/conversations/:id/messages/stream` | 流式聊天 (SSE) | ✅ |
| GET | `/api/v1/tools` | 列出可用工具 | ✅ |
| GET | `/api/v1/providers` | 列出 LLM providers | ✅ |

---

## 已添加依赖

### 后端
```go
// go.mod 新增
modernc.org/sqlite                  // 纯 Go SQLite ✅
github.com/google/uuid              // UUID 生成 ✅
```

---

## 完成状态

| 模块 | 状态 |
|------|------|
| LLM Provider 接口 | ✅ 完成 |
| 工具/函数调用 | ✅ 完成 |
| 流式支持 | ✅ 完成 |
| 上下文管理 | ✅ 完成 |
| 记忆/历史 | ✅ 完成 |
| KV 存储 | ✅ 完成 |
| 聊天 API | ✅ 完成 |
| 聊天 UI | ✅ 完成 |
| 设置 UI | ✅ 完成 |
| 重试逻辑 | ✅ 完成 |
| 速率限制 | ✅ 完成 |
| 后端单元测试 | ✅ 86/86 通过 |
| 前端单元测试 | ✅ 48/48 通过 |

---

---

## Clawdbot 功能对齐（新增自差距分析）

### 9. LLM 模型故障转移

**目标**：当主模型失败时，在配置的模型之间自动回退。

#### 后端任务
- [x] **故障转移链** (`server/internal/llm/`)
  - [x] 模型优先级配置 (`model_chain.go`)
  - [x] 错误时自动故障转移
  - [x] 速率限制时故障转移
  - [x] 超时时故障转移
  - [x] 每个模型的熔断器（通过健康跟踪）

- [x] **健康监控**
  - [x] 模型健康检查端点 (`llm_health_handler.go`)
  - [x] 响应时间跟踪
  - [x] 错误率跟踪
  - [x] 自动恢复检测

- [x] **故障转移策略**
  - [x] 立即故障转移
  - [x] 重试后故障转移
  - [x] 轮询负载均衡（通过优先级）
  - [x] 加权分布

- [x] **配置** (`config/llm_config.go`)
  - [x] 配置中的模型链定义
  - [x] 每个 Agent 的模型偏好
  - [x] 回退模型设置
  - [x] 每个模型的超时配置

- [x] **错误分类** (`error_classifier.go`)
  - [x] 可重试 vs 不可重试错误
  - [x] Provider 宕机检测
  - [x] 配额超限处理
  - [x] 上下文过长处理

#### 测试
- [ ] 故障转移链测试
- [ ] 熔断器测试
- [ ] 健康监控测试

---

### 10. 高级会话管理

**目标**：实现复杂的会话处理，包括压缩、修剪和隔离。

#### 后端任务
- [x] **会话压缩** (`server/internal/session/`)
  - [x] 压缩前内存刷新以持久化笔记
  - [x] LLM 调用前自动上下文修剪
  - [x] 带消息摘要的安全修剪 (`compactor.go`)
  - [x] 可配置的压缩阈值
  - [x] 上下文窗口管理的 Token 计数

- [x] **会话隔离**
  - [x] 每个 Agent 的会话隔离
  - [x] 每个通道的会话隔离
  - [x] 每个对等方（用户）的会话隔离
  - [x] 会话键格式：`agent:channel:peer:thread`
  - [x] 会话重置策略（手动、超时、命令）

- [x] **会话持久化** (`persistence.go`)
  - [x] 会话状态序列化
  - [x] 重启时会话恢复
  - [x] 会话导出/导入（通过 SQLite）
  - [x] 会话清理调度器

- [x] **API 端点** (`session_handler.go`)
  - [x] `GET /api/v1/sessions` - 列出会话
  - [x] `GET /api/v1/sessions/:id` - 获取会话详情
  - [x] `POST /api/v1/sessions/:id/reset` - 重置会话
  - [x] `POST /api/v1/sessions/:id/compact` - 强制压缩
  - [x] `POST /api/v1/sessions/:id/archive` - 归档会话
  - [x] `DELETE /api/v1/sessions/:id` - 删除会话
  - [x] `GET /api/v1/sessions/stats` - 会话统计

#### 测试
- [ ] 会话压缩测试
- [ ] 会话隔离测试
- [ ] 会话持久化测试

---

### 11. 混合记忆系统

**目标**：实现向量嵌入 + 关键词搜索，用于智能记忆检索。

#### 后端任务
- [x] **向量嵌入** (`server/internal/embedding/`)
  - [x] 嵌入提供者接口 (`provider.go`)
  - [x] OpenAI 嵌入提供者 (`openai.go`)
  - [x] 本地嵌入 - Ollama (`ollama.go`)
  - [x] SQLite 中的嵌入存储 (`cache.go`)
  - [x] 批量嵌入生成

- [x] **混合搜索** (`server/internal/memory/`)
  - [x] 向量相似度搜索 - 余弦、点积、欧几里得 (`vector_store.go`)
  - [x] 关键词/FTS5 搜索
  - [x] 混合排序 - 加权组合 (`hybrid_search.go`)
  - [x] 搜索结果去重
  - [x] 相关性阈值过滤

- [x] **记忆管理**
  - [x] 压缩前自动刷新记忆
  - [x] 按年龄/相关性修剪记忆
  - [x] 记忆标记和分类（通过元数据）
  - [x] 记忆重要性评分（通过相似度）
  - [ ] 跨会话记忆共享（可选）- 推迟

- [x] **API 端点** (`memory_handler.go`)
  - [x] `POST /api/v1/memory/store` - 存储记忆
  - [x] `POST /api/v1/memory/search` - 搜索记忆
  - [x] `GET /api/v1/memory/:id` - 获取记忆
  - [x] `DELETE /api/v1/memory/:id` - 删除记忆
  - [x] `POST /api/v1/memory/prune` - 修剪旧记忆
  - [x] `DELETE /api/v1/memory` - 清除所有记忆
  - [x] `GET /api/v1/memory/stats` - 记忆统计

#### 测试
- [ ] 嵌入生成测试
- [ ] 向量搜索测试
- [ ] 混合搜索测试
- [ ] 记忆管理测试

---

## 已创建文件（Clawdbot 功能对齐）

### LLM 故障转移
| 文件 | 状态 | 描述 |
|------|------|------|
| `internal/config/llm_config.go` | ✅ | LLM、会话、嵌入、记忆配置 |
| `internal/llm/error_classifier.go` | ✅ | 用于重试/故障转移的错误分类 |
| `internal/llm/model_chain.go` | ✅ | 带故障转移的模型链管理 |
| `internal/server/llm_health_handler.go` | ✅ | 健康检查 API 端点 |

### 会话管理
| 文件 | 状态 | 描述 |
|------|------|------|
| `internal/session/session.go` | ✅ | 会话类型和状态管理 |
| `internal/session/persistence.go` | ✅ | SQLite 会话持久化 |
| `internal/session/manager.go` | ✅ | 会话生命周期管理 |
| `internal/session/compactor.go` | ✅ | 带 LLM 摘要的会话压缩 |
| `internal/server/session_handler.go` | ✅ | 会话 API 端点 |

### 混合记忆
| 文件 | 状态 | 描述 |
|------|------|------|
| `internal/embedding/provider.go` | ✅ | 嵌入提供者接口 |
| `internal/embedding/openai.go` | ✅ | OpenAI 嵌入 |
| `internal/embedding/ollama.go` | ✅ | Ollama 本地嵌入 |
| `internal/embedding/cache.go` | ✅ | 带 SQLite 的嵌入缓存 |
| `internal/memory/vector_store.go` | ✅ | 带 FTS5 的向量存储 |
| `internal/memory/hybrid_search.go` | ✅ | 混合向量 + 关键词搜索 |
| `internal/server/memory_handler.go` | ✅ | 记忆 API 端点 |

---

## 下一步 (v0.3.0)
1. systemd / launchd 支持
2. cgroup / rlimit 资源限制
3. 文件系统能力（watch / IO 任务）
4. Docker 构建配置
5. CI/CD 流水线
6. 每客户端速率限制
7. 外部 API 重试逻辑
