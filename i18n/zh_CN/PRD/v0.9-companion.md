# PRD: Echo Companion - 实时 Agent 监控

**状态:** 草稿
**作者:** ZimaOS 团队
**最后更新:** 2026-01-28
**目标版本:** v0.9

---

## 概述

Echo Companion 是一个实时伴侣监控工具，为开发者和管理员提供可视化界面来观察 AI Agent 在多个平台上的运作情况。参考 [Crabwalk](https://github.com/luccast/crabwalk) 的设计理念，结合 ZimaOS Echo 项目对安全的深度洞察，提供一个安全、可观测、可审计的 Agent 监控解决方案。

### 核心价值

1. **实时可观测性**: 实时追踪 Agent 的会话、工具调用和决策链路
2. **安全审计**: 结合 Echo 的安全层，提供威胁检测和异常行为告警
3. **跨平台监控**: 统一监控 WhatsApp、Telegram、Discord、Slack、Matrix、飞书等渠道
4. **调试友好**: 帮助开发者快速定位问题和优化 Agent 行为

## 目标

- [x] 提供实时 Agent 活动可视化界面
- [x] 集成 Echo 现有安全层（Prompt Guard、Audit Log、Sandbox）
- [x] 支持多平台 Agent 会话监控
- [x] 提供威胁检测和异常行为告警
- [x] 支持会话回放和调试分析

## 用户故事

### 故事 1: 实时活动监控

**作为** 系统管理员
**我希望** 实时查看所有 Agent 的活动状态
**以便** 我可以及时发现异常行为并采取措施

**验收标准:**
- [ ] 可视化展示当前活动的 Agent 会话
- [ ] 实时更新会话状态和工具调用
- [ ] 支持按平台、用户、时间筛选
- [ ] 显示会话的安全评分和威胁等级

### 故事 2: 安全威胁告警

**作为** 安全工程师
**我希望** 在检测到 Prompt Injection 或异常行为时收到告警
**以便** 我可以快速响应安全事件

**验收标准:**
- [ ] 集成 Prompt Guard 威胁检测结果
- [ ] 实时显示威胁等级（None/Low/Medium/High/Critical）
- [ ] 支持配置告警阈值和通知方式
- [ ] 提供威胁详情和建议处置措施

### 故事 3: 会话调试与回放

**作为** 开发者
**我希望** 回放和分析历史会话
**以便** 我可以调试和优化 Agent 行为

**验收标准:**
- [ ] 支持会话时间线回放
- [ ] 展示完整的工具调用链和参数
- [ ] 显示 LLM 输入输出和 Token 消耗
- [ ] 支持导出会话数据用于分析

### 故事 4: 操作链可视化

**作为** 开发者
**我希望** 以图形化方式查看 Agent 的操作链
**以便** 我可以理解 Agent 的决策过程

**验收标准:**
- [ ] 使用流程图展示操作链
- [ ] 支持展开节点查看详细参数
- [ ] 显示每个操作的执行时间和状态
- [ ] 支持缩放和拖拽交互

## 需求

### 功能需求

| ID | 需求 | 优先级 | 备注 |
|----|------|--------|------|
| FR-001 | WebSocket 实时数据流 | P0 | 与 Echo Gateway 建立持久连接 |
| FR-002 | 会话列表和筛选 | P0 | 按平台、用户、时间、状态筛选 |
| FR-003 | 操作链流程图可视化 | P0 | 使用 ReactFlow 或类似库 |
| FR-004 | 威胁检测集成 | P0 | 集成 Prompt Guard 结果 |
| FR-005 | 审计日志集成 | P0 | 集成 Audit Log 数据 |
| FR-006 | 实时告警通知 | P1 | 支持 WebSocket/Webhook 通知 |
| FR-007 | 会话回放功能 | P1 | 时间线回放历史会话 |
| FR-008 | 工具调用详情展示 | P1 | 展示参数、返回值、耗时 |
| FR-009 | Sandbox 执行监控 | P1 | 监控沙箱执行状态和资源使用 |
| FR-010 | 多租户支持 | P2 | 支持租户隔离和权限控制 |
| FR-011 | 数据导出 | P2 | 导出 JSON/CSV 格式 |
| FR-012 | 自定义仪表盘 | P2 | 支持自定义监控面板 |

### 非功能需求

| ID | 需求 | 目标 | 备注 |
|----|------|------|------|
| NFR-001 | WebSocket 延迟 | < 100ms | 实时性要求 |
| NFR-002 | 页面加载时间 | < 2s | 首屏加载 |
| NFR-003 | 并发会话支持 | 1000+ | 同时监控的会话数 |
| NFR-004 | 数据保留期 | 7 天 | 默认保留期，可配置 |
| NFR-005 | 浏览器兼容性 | Chrome/Firefox/Safari | 最新两个版本 |
| NFR-006 | 数据可读性 | 优先使用纯文本文件，便于用户阅读 | 避免使用数据库存储 |

## 技术设计

### 架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Echo Companion 架构                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                        前端 (Web UI)                                 │    │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌────────────┐  │    │
│  │  │   仪表盘     │ │   会话       │ │   流程图     │ │   告警     │  │    │
│  │  │   概览       │ │   浏览器     │ │   查看器     │ │   中心     │  │    │
│  │  └──────────────┘ └──────────────┘ └──────────────┘ └────────────┘  │    │
│  │                                                                      │    │
│  │  技术栈: Vue 3 + TypeScript + TailwindCSS + ReactFlow               │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                    │                                         │
│                                    │ WebSocket / REST API                    │
│                                    ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                     Echo 后端 (Go)                                   │    │
│  │  ┌──────────────────────────────────────────────────────────────┐   │    │
│  │  │                  Companion 服务                               │   │    │
│  │  │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌───────────┐  │   │    │
│  │  │  │   会话     │ │   事件     │ │   告警     │ │   回放    │  │   │    │
│  │  │  │   管理器   │ │   流处理   │ │   引擎     │ │   服务    │  │   │    │
│  │  │  └────────────┘ └────────────┘ └────────────┘ └───────────┘  │   │    │
│  │  └──────────────────────────────────────────────────────────────┘   │    │
│  │                                │                                     │    │
│  │  ┌─────────────────────────────┴────────────────────────────────┐   │    │
│  │  │                  安全集成                                     │   │    │
│  │  │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌───────────┐  │   │    │
│  │  │  │  Prompt    │ │   审计     │ │   沙箱     │ │   认证    │  │   │    │
│  │  │  │  Guard     │ │   日志     │ │   监控     │ │  (OIDC)   │  │   │    │
│  │  │  └────────────┘ └────────────┘ └────────────┘ └───────────┘  │   │    │
│  │  └──────────────────────────────────────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                    │                                         │
│                                    ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                     数据源                                           │    │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────────────┐  │    │
│  │  │   渠道     │ │   LLM      │ │   工具     │ │     审计         │  │    │
│  │  │   事件     │ │   请求     │ │   调用     │ │     日志         │  │    │
│  │  │ (WA/TG/...)│ │            │ │            │ │                  │  │    │
│  │  └────────────┘ └────────────┘ └────────────┘ └──────────────────┘  │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 数据模型

#### 会话事件

```go
type SessionEvent struct {
    ID          string                 `json:"id"`
    SessionID   string                 `json:"session_id"`
    Timestamp   time.Time              `json:"timestamp"`
    EventType   SessionEventType       `json:"event_type"`
    Platform    string                 `json:"platform"`    // whatsapp, telegram, discord 等
    UserID      string                 `json:"user_id"`
    TenantID    string                 `json:"tenant_id,omitempty"`

    // 事件特定数据
    Message     *MessageEvent          `json:"message,omitempty"`
    ToolCall    *ToolCallEvent         `json:"tool_call,omitempty"`
    LLMRequest  *LLMRequestEvent       `json:"llm_request,omitempty"`
    Security    *SecurityEvent         `json:"security,omitempty"`

    // 元数据
    Duration    time.Duration          `json:"duration,omitempty"`
    Status      string                 `json:"status"`
    Error       string                 `json:"error,omitempty"`
}

type SessionEventType string

const (
    EventSessionStart    SessionEventType = "session_start"
    EventSessionEnd      SessionEventType = "session_end"
    EventMessageReceived SessionEventType = "message_received"
    EventMessageSent     SessionEventType = "message_sent"
    EventToolCall        SessionEventType = "tool_call"
    EventLLMRequest      SessionEventType = "llm_request"
    EventSecurityThreat  SessionEventType = "security_threat"
    EventSandboxExec     SessionEventType = "sandbox_exec"
)
```

#### 安全事件

```go
type SecurityEvent struct {
    ThreatLevel     string   `json:"threat_level"`     // none, low, medium, high, critical
    ThreatScore     int      `json:"threat_score"`
    ThreatTypes     []string `json:"threat_types"`
    DetectedPatterns []string `json:"detected_patterns"`
    Action          string   `json:"action"`           // allowed, blocked, filtered
    FilteredContent string   `json:"filtered_content,omitempty"`
}
```

#### 工具调用事件

```go
type ToolCallEvent struct {
    ToolName    string                 `json:"tool_name"`
    ToolID      string                 `json:"tool_id"`
    Input       map[string]interface{} `json:"input"`
    Output      interface{}            `json:"output,omitempty"`
    Duration    time.Duration          `json:"duration"`
    Status      string                 `json:"status"`
    SandboxUsed bool                   `json:"sandbox_used"`
    Resources   *ResourceUsage         `json:"resources,omitempty"`
}

type ResourceUsage struct {
    CPUTime     time.Duration `json:"cpu_time"`
    MemoryPeak  int64         `json:"memory_peak"`
    IORead      int64         `json:"io_read"`
    IOWrite     int64         `json:"io_write"`
}
```

### API 端点

#### WebSocket API

| 端点 | 描述 |
|------|------|
| `ws://host/api/v1/companion/stream` | 实时事件流 |
| `ws://host/api/v1/companion/session/:id` | 单会话事件流 |

#### REST API

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/v1/companion/sessions` | 获取会话列表 |
| GET | `/api/v1/companion/sessions/:id` | 获取会话详情 |
| GET | `/api/v1/companion/sessions/:id/events` | 获取会话事件 |
| GET | `/api/v1/companion/sessions/:id/flow` | 获取操作链数据 |
| GET | `/api/v1/companion/alerts` | 获取告警列表 |
| PUT | `/api/v1/companion/alerts/:id/ack` | 确认告警 |
| GET | `/api/v1/companion/stats` | 获取统计数据 |
| GET | `/api/v1/companion/export` | 导出数据 |

### 安全集成

#### Prompt Guard 集成

```go
// 在消息处理流程中集成 Companion 事件
func (s *CompanionService) OnPromptGuardResult(
    sessionID string,
    input string,
    result *promptguard.CheckResult,
) {
    if result.IsThreat {
        s.EmitEvent(SessionEvent{
            SessionID: sessionID,
            EventType: EventSecurityThreat,
            Security: &SecurityEvent{
                ThreatLevel:      result.ThreatLevel.String(),
                ThreatScore:      result.Score,
                ThreatTypes:      result.ThreatTypes,
                DetectedPatterns: result.Patterns,
                Action:           "blocked",
            },
        })

        // 触发告警
        if result.ThreatLevel >= promptguard.ThreatHigh {
            s.alertEngine.Trigger(Alert{
                Severity: "high",
                Title:    "检测到高危 Prompt Injection",
                SessionID: sessionID,
                Details:  result,
            })
        }
    }
}
```

#### 审计日志集成

```go
// 订阅审计日志事件
func (s *CompanionService) SubscribeAuditEvents() {
    s.auditSubscriber.Subscribe(func(entry *audit.Entry) {
        // 转换为 Companion 事件
        event := s.convertAuditEntry(entry)
        s.EmitEvent(event)
    })
}
```

#### 沙箱监控集成

```go
// 监控沙箱执行
func (s *CompanionService) OnSandboxExecution(
    sessionID string,
    exec *sandbox.Execution,
) {
    s.EmitEvent(SessionEvent{
        SessionID: sessionID,
        EventType: EventSandboxExec,
        ToolCall: &ToolCallEvent{
            ToolName:    exec.Command,
            SandboxUsed: true,
            Duration:    exec.Duration,
            Status:      exec.Status,
            Resources: &ResourceUsage{
                CPUTime:    exec.CPUTime,
                MemoryPeak: exec.MemoryPeak,
            },
        },
    })
}
```

### 前端组件

#### 仪表盘概览

- 活跃会话数量和趋势图
- 平台分布饼图
- 威胁等级分布
- 最近告警列表
- 系统健康状态

#### 会话浏览器

- 会话列表（支持筛选和搜索）
- 会话详情面板
- 消息时间线
- 工具调用列表

#### 流程图查看器

- ReactFlow 操作链可视化
- 节点类型：消息、工具调用、LLM 请求、安全检查
- 支持展开/折叠节点详情
- 支持缩放和拖拽

#### 告警中心

- 告警列表（按严重程度排序）
- 告警详情和处置建议
- 告警确认和静默功能
- 告警规则配置

### 配置

```yaml
companion:
  enabled: true

  # WebSocket 配置
  websocket:
    ping_interval: 30s
    write_timeout: 10s
    read_buffer_size: 1024
    write_buffer_size: 1024

  # 数据保留
  retention:
    events_days: 7
    sessions_days: 30
    alerts_days: 90

  # 告警配置
  alerts:
    enabled: true
    threat_threshold: medium  # 触发告警的最低威胁等级
    channels:
      - type: webhook
        url: "https://your-webhook.com/alerts"
      - type: email
        recipients:
          - "admin@example.com"

  # 安全集成
  security:
    prompt_guard_integration: true
    audit_log_integration: true
    sandbox_monitor: true

  # 性能配置
  performance:
    max_concurrent_sessions: 1000
    event_buffer_size: 10000
    batch_write_interval: 1s
```

## 安全考虑

### 认证与授权

- 使用 Echo 现有的 OIDC 认证
- 基于角色的访问控制（RBAC）
  - `companion:read` - 查看会话和事件
  - `companion:admin` - 管理告警和配置
- 多租户隔离

### 数据隐私

- 敏感数据脱敏（API Key、密码等）
- 可配置的数据保留策略
- 支持数据导出和删除（GDPR 合规）

### 威胁检测增强

Companion 可以提供额外的安全洞察：

1. **异常行为检测**
   - 异常高频的工具调用
   - 异常的会话时长
   - 异常的资源使用

2. **跨会话关联分析**
   - 识别来自同一来源的攻击尝试
   - 检测分布式攻击模式

3. **实时威胁情报**
   - 聚合威胁检测结果
   - 生成威胁趋势报告

## 里程碑

| 阶段 | 范围 | 目标日期 |
|------|------|----------|
| 阶段 1 | 核心架构和实时事件流 | 第 1-2 周 |
| 阶段 2 | 前端 Dashboard 和 Session Explorer | 第 3-4 周 |
| 阶段 3 | Flow Viewer 和安全集成 | 第 5-6 周 |
| 阶段 4 | Alert Center 和高级功能 | 第 7-8 周 |

## 成功指标

| 指标 | 目标 | 测量方法 |
|------|------|----------|
| 事件延迟 | < 100ms | P99 延迟监控 |
| 威胁检测准确率 | > 95% | 人工审核采样 |
| 用户满意度 | > 4.0/5.0 | 用户反馈调查 |
| 系统可用性 | > 99.9% | 监控告警 |

## 待解决问题

- [ ] 是否需要支持自定义告警规则（DSL）？
- [ ] 是否需要集成外部 SIEM 系统？
- [ ] 历史数据的存储方案（SQLite vs TimescaleDB）？
- [ ] 是否需要支持移动端访问？

## 参考资料

- [Crabwalk](https://github.com/luccast/crabwalk) - 设计参考
- [ReactFlow](https://reactflow.dev/) - 流程图可视化库
- [Echo 安全文档](../docs/guide/security.md) - 安全架构
- [OWASP LLM Top 10](https://owasp.org/www-project-top-10-for-large-language-model-applications/) - LLM 安全指南
